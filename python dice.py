import rabdom

while True:
  choice=input('Roll the dice(y/n):')
               if choice==y:
               die1=random.randit(1,6)
               die1=random.randit(1,6)
          elif choice==n:
               print('Thanks for playing')
            break
         else:
              pront('invalid choice')
